# 数据与授权说明

本仓库不包含原始数据文件。

## 未包含的数据

- CSMAR、Wind、iFinD、RESSET 等数据库导出的授权数据；
- 股票日度或月度行情原始文件；
- 财务报表和财务指标原始数据；
- 指数收益率和宏观金融变量原始数据；
- 研报 PDF 和其他受版权保护的材料。

## 本地数据路径

部分 Notebook 中可能仍包含类似如下的本地路径：

```text
D:/stata/...
D:/python/references/...
```

如果需要重新运行 Notebook，请将这些路径替换为你自己的本地数据目录。更规范的做法是将路径集中放在开头配置单元中，例如：

```python
from pathlib import Path

DATA_DIR = Path("../data")
```

## 可复现性说明

由于原始数据不能随仓库一同分发，本仓库更适合作为研究代码和作品集展示。部分项目保留了图表和结果文件，便于快速查看；若要完全复现，需要准备等价的数据源。
